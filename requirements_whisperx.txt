# WhisperX with Blackwell GPU Support (PR #1182)
# Python 3.11+ required

# PyTorch 2.7.1 with CUDA 12.8 support (Blackwell compatible)
--index-url https://download.pytorch.org/whl/cu128
torch==2.7.1
torchaudio
torchvision

# WhisperX from PR #1182 (commit 0e7153b)
git+https://github.com/m-bain/whisperx.git@0e7153b

# Core dependencies for whisper-api
faster-whisper==1.0.3
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
aiofiles==24.1.0
pydub==0.25.1
numpy<2.0
librosa
soundfile

# Additional WhisperX dependencies
ctranslate2>=4.5.0
triton>=3.3.0; platform_system != "Windows"

# Optional for better performance
numba
accelerate
transformers